/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/CtrlLanding",
  output: 'export',
  distDir: 'output',
  devIndicators: {
    buildActivity: false,
  },
};

export default nextConfig;
